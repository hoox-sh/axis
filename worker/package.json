{
  "name": "axis-worker",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "author": "jango_blockchained",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:pages": "wrangler pages deploy ../dist --project-name=pynescript-superchart",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "description": "AXIS Cloudflare Worker (API, WebSocket, D1)",
  "repository": {
    "type": "git",
    "url": "https://github.com/jango-blockchained/axis.git"
  }
}
